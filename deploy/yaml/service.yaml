apiVersion: v1
kind: Service
metadata:
  name: java-demoapp
  namespace: demoapp    
spec:
  type: ClusterIP
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: http
  selector:
    app: java-demoapp